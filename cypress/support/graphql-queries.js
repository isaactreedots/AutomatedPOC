export const allCountries = `
query allCountries {
    allCountries {
        active,
        currency_code,
        currency_symbol,
        description,
        id,
        name,
    }
}`;

export const allMenus = `
query allMenus {
    allMenus {
        active,
        description,
        icon,
        id,
        label,
        menu_id,
        name,
        to,
    }
}`;

